# 数据结构

## 时间复杂度与空间复杂度

## 线性表的顺序表示

## 线性表的链式表示

## 栈和队列的基本性质

## 栈和队列的存储结构

## 双端队列

## 栈与队列的应用

**出栈顺序**
卡特兰数 $C_n\cfrac{1}{n+1}\cfrac{(2n)!}{(n!)^2}$
比$a_i$小的需要严格递减

## 多维数组的存储

## 特殊矩阵的压缩存储

## 串的模式匹配算法

## 树的基本性质

## 二叉树的定义与性质

完全二叉树
- $n_1=0/1$
- 若有$2k/2k+1$个结点，则有k个叶子节点，k-1个$n_2$

## 二叉树的遍历

## 树、森林与二叉树的转换

**树→二叉树**
1. 画出根节点
2. 找到同一层的所有孩子
3. 斜向右下画出，第一个孩子在下一层，第二个孩子在下两层
4. 连接第一个孩子作为根节点左孩子

**森林→二叉树**（左孩子右兄弟）

**二叉树→森林**

## 线索二叉树的基本概念和构造

## 哈夫曼树与哈夫曼编码

**树**
选择当前最小的两个值作为本次的节点并删去，求和后的值加入待选池中。
WPL：叶节点*深度（以根节点为0开始）

**编码**
每个节点左子树编码为0，右子树编码为1，孩子的编码累加在其后

## 并查集

## 图的基本概念

## 图的存储及基本操作

## 图的遍历

## 最小生成树

**普里姆(Prim)算法**
从某一个顶点开始构建生成树；每次将代价最小的新顶点纳入生成树，直到所有顶点都纳入为止。

**克鲁斯卡尔(Kruskal)算法**
每次选择一条权值最小的边，使这条边的两头连通（原本已经连通的就不选），直到所有节点都连通。

## 最短路径

**广度优先(BFS)算法**
**无权图**的单源最短路径

**深度优先(DFS)算法**


**迪杰斯特拉(Dijkstra)算法**（无法处理带负权值图）
带权图的单源最短路径

**弗洛伊德(Floyd)算法**
带权图中各定点间的最短路径

## 拓扑排序

入度为0的定点

## 关键路径



## 有向无环图存储算数表达式

## 顺序查找法&折半查找法

折半查找
以2为底，向上取整

## 二叉排序树

## 平衡二叉树

- 任一结点的左子树和右子树深度之差不超过1

## 红黑树

## B树（多路平衡查找树）及其基本操作、B+树的基本概念

1）树中每个结点至多有$m$棵子树，即至多含有$m-1$个关键字。
2）若根结点不是终端结点，则至少有两棵子树。
3）除根结点外的所有非叶结点至少有 $\cfrac{m}{2}$ 棵子树，即至少含有 $\cfrac{m}{2}-1$ 个关键字。
5）所有的叶结点都出现在同一层次上，并且不带信息（可以视为外部结点或类似于折半查找判定树的查找失败结点，实际上这些结点不存在，指向这些结点的指针为空）

## 散列(Hash)表

## 插入排序

直接插入
折半插入

## 交换排序

## 快速排序
将所有数升序排列，与位置相等的为枢轴

## 选择排序

## 二路归并排序

## 基数排序

## 各种内部排序方法的比较

不稳定：**选**（选择排序）**艾希**（希尔排序）**堆**（堆排序）**攻速**（快速排序）

|算法种类|最好情况|平均情况|最坏情况|空间复杂度|是否稳定|基于比较|
|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
|插入排序|$O(n)$|$O(n^2)$|$O(n^2)$|$O(1)$|是|是|
|希尔排序||||$O(1)$|否|是|
|选择排序|$O(n^2)$|$O(n^2)$|$O(n^2)$|$O(1)$|否|是|
|堆排序|$O(nlog_2n)$|$O(nlog_2n)$|$O(nlog_2n)$|$O(1)$|否|是|
|冒泡排序|$O(n)$|$O(n^2)$|$O(n^2)$|$O(1)$|是|是|
|快速排序|$O(nlog_2n)$|$O(nlog_2n)$|$O(n^2)$|$O(log_2n)$|否|是|
|二路归并排序|$O(nlog_2n)$|$O(nlog_2n)$|$O(nlog_2n)$|$O(n)$|是|是|
|基数排序|$O[d(n+r)]$|$O[d(n+r)]$|$O[d(n+r)]$|$O(r)$|是|否|

## 外部排序的思想

# 计算机组成原理

## 计算机系统层次结构

## 计算机的性能指标

- 每条指令所需的时钟周期数**CPI**：
- CPU耗时：$指令数量*\frac{CPI}{主频}$
- 每秒执行多少条指令**IPS**：$\frac{主频}{CPI}$
- 浮点运算**FLOPS**

## 定点数的表示与运算

① 确定**位数**，计算**模**
`short` $2$字节$=16$位，模$65536=10000_H$
② 直接求补码，符号位置$0$（正数）/模$-$绝对值，符号位置$1$（负数）
> 转化为16进制再计算，取余反序

## C语言中各种数据的转换

## IEEE754标准，浮点数运算

## 数据的对齐和大小端存储

## 半导体随机存取存储器

## 主存储器与CPU的连接

## 低位交叉存储器

## 高速缓冲存储器（Cache）

## 虚拟存储器

## 磁盘存储器

## 指令格式

## 指令的寻址方式

## CISC与RISC

## 程序的机器级代码表示

## CPU的功能和基本结构

## 指令执行过程

## 数据通路的功能和基本结构

## 控制器的功能和工作原理

## 指令流水线

## 多处理器的基本概念

## 总线概念和常见总线标准

## 总线的性能指标

## 外部设备和I/O接口

## 程序查询方式

## 程序中断方式

## DMA方式

## 加法器

## 乘法电路

## 除法电路

# 操作系统

## 操作系统的概念、特征和功能

## 内核态与用户态

## 中断、异常

## 系统调用

## 操作系统引导

## 进程与线程

## 进程状态与进程控制

## 处理机调度

## 进程同步与互斥

## 经典同步问题（今年注意读者/写者）

## 死锁

## 内存管理的概念

## 连续分配管理方式

## 非连续分配管理方式

## 虚拟页式存储管理

## 文件元数据和索引节点

## 文件的操作

## 文件的逻辑结构和物理结构

## 文件共享和文件保护

## 目录结构和操作

## 磁盘的组织与管理

## I/O控制方式

## I/O软件的层次结构

## 高速缓存与缓冲区

## 设备分配与回收

## SPOOLing技术

# 计算机网络

## 📌计算机网络的分层结构

## 通信基础概念

## 📌奈氏准则&香农定理

## 编码与调制

## 数据交换方式

## 物理层设备&传输介质&接口特性

## 差错控制

## 📌流量控制&可靠传输&滑动窗口

停止等待协议
选择重传协议
后退 N 帧协议

发送窗口-接收窗口

信道利用率

## 介质访问控制

📌 CSMA/CA
📌CSMA/CD

## 局域网&广域网(VLAN)

## 链路层设备

## 网络层功能 (SDN)

## IPv4分组

## 📌IPv4地址与NAT

## 📌子网掩码&子网划分

## 📌CIDR&路由聚合

## 📌ARP、DHCP、ICMP协议

## IPv6

## 路由协议：RIP、OSPF、BGP

## IP组播

## 移动IP

## 网络层设备

## UDP协议&UDP数据报

## TCP报文段

## 📌TCP连接管理

三次握手
四次挥手
标志位变化

## 📌TCP可靠传输

## 📌TCP流量控制

接收窗口
发送窗口

## 📌TCP拥塞控制

慢开始
拥塞避免
快重传

## 网络应用模型

## 域名解析

## 文件传输协议FTP

## 电子邮件系统及相关协议
POP3
IMAP

## WWW与HTTP协议